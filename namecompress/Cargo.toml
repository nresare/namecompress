[package]
name = "namecompress"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Compression of personal names against a separately distributed model"

[dependencies]
